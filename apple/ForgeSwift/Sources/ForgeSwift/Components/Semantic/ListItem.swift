//
//  ListItem.swift
//  ForgeSwift
//
//  TODO: standard row component with leading (icon/avatar), title
//  + subtitle, trailing (chevron/badge/switch). Built as a
//  composition of HStack + Text + optional widgets. Most list UIs
//  use this as the primary row type.
//

public struct ListItem: ComposedView {
    public init() {}

    public func build(context: BuildContext) -> any View {
        Text("TODO: ListItem")
    }
}
