//
//  AnimatedTransform.swift
//  ForgeSwift
//
//  TODO: animated wrapper that transitions its child's visual
//  transform (translate/scale/rotate) over time. Requires the
//  animation infrastructure we've deferred — not implementable
//  until there's an animation primitive to hook into.
//

public struct AnimatedTransform: ComposedView {
    public init() {}

    public func build(context: BuildContext) -> any View {
        Text("TODO: AnimatedTransform")
    }
}
