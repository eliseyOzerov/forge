#if canImport(UIKit)
import UIKit

// MARK: - Example App Usage
// This shows how developers would use your framework

/*
@main
class MyApp: App {
    override var body: some Scene {
        WindowScene {
            ContentView()
        }
    }
    
    // Optional: Override specific delegate methods for custom behavior
    override func applicationDidEnterBackground(_ application: UIApplication) {
        print("App entered background")
        // Custom background handling
    }
    
    override func application(_ application: UIApplication, didReceiveRemoteNotification userInfo: [AnyHashable : Any]) {
        print("Received push notification: \(userInfo)")
        // Push notification handling
    }
}

struct ContentView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Hello, SwiftKit!")
                .font(.largeTitle)
                .foregroundColor(.systemBlue)
            
            Text("A declarative UIKit library")
                .font(.body)
                .foregroundColor(.secondaryLabel)
        }
    }
}
*/

#endif
